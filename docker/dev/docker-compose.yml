version: '3'
services:

  config-server:
    ports:
      - "8888:8888"

  eureka-server:
    ports:
      - "8761:8761"

  database:
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "password"
      POSTGRES_DB:       "jsource"